# Backend API URL (auto-configured by Render)
VITE_API_URL=http://localhost:8000

# For production, Render will automatically inject this from the backend service
# No need to set manually when deploying via render.yaml
